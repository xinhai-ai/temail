# ======================
# Database
# ======================
# Copy to `.env` and uncomment the variables you need.
#
# SQLite (default, recommended for dev):
#   file:./dev.db
#
# Docker + volume example (recommended for compose with SQLite):
#   file:/data/temail.db
#
# PostgreSQL (recommended for production):
#   postgresql://user:password@localhost:5432/temail
#
# DATABASE_URL="file:./dev.db"
# DATABASE_URL="postgresql://user:password@localhost:5432/temail"

# ======================
# Auth (NextAuth v5)
# ======================
# Generate a strong secret for production:
#   openssl rand -hex 32
# AUTH_SECRET="change-me"

# The public base URL of your site (no trailing slash).
# Local dev:
# AUTH_URL="http://localhost:3000"

# ======================
# App (public)
# ======================
# NEXT_PUBLIC_APP_NAME="TEmail"
# NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ======================
# Bootstrap Super Admin (optional)
# ======================
# If set, the first registered user can become SUPER_ADMIN by providing this value as `adminSecret`.
# BOOTSTRAP_SUPER_ADMIN_SECRET=""

# ======================
# Bootstrap Admin (auto-created)
# ======================
# Docker Compose will run `node scripts/bootstrap-admin.js` on startup.
# If there are no users in the database, it creates a SUPER_ADMIN user.
#
# BOOTSTRAP_ADMIN_EMAIL="admin@temail.local"
# BOOTSTRAP_ADMIN_PASSWORD=""
# BOOTSTRAP_ADMIN_NAME="Admin"
# BOOTSTRAP_ADMIN_ROLE="SUPER_ADMIN"
#
# Used by the bootstrap script wait mode:
# BOOTSTRAP_WAIT_SECONDS="180"
# BOOTSTRAP_POLL_INTERVAL_MS="1000"

# ======================
# SMTP (optional, for email forwarding)
# ======================
# SMTP_HOST=""
# SMTP_PORT="587"
# SMTP_SECURE="false"
# SMTP_USER=""
# SMTP_PASS=""
# SMTP_FROM=""

# ======================
# IMAP Service (optional)
# ======================
# Enable IMAP Service integration in the Web app.
# IMAP_SERVICE_ENABLED="1"

# Where the Web app reaches the IMAP Service.
# Local dev: localhost
# Docker Compose: worker
# IMAP_SERVICE_HOST="localhost"
# IMAP_SERVICE_HOST="worker"
# IMAP_SERVICE_PORT="3001"

# Shared secret between IMAP Service and Web internal API `/api/internal/realtime`.
# Leave empty to only allow localhost calls (dev only).
# IMAP_SERVICE_KEY=""

# IMAP Service -> Web callback base URL (used for realtime notifications).
# NEXTJS_URL="http://localhost:3000"
# NEXTJS_PORT="3000"

# IMAP Service tuning (optional)
# IMAP_IDLE_TIMEOUT_MS="1500000"
# IMAP_HEARTBEAT_MS="300000"
# IMAP_RECONCILE_MS="30000"
# IMAP_FULL_SYNC_MS="300000"
# IMAP_HEALTH_CHECK_MS="60000"
# IMAP_RECONNECT_MIN_MS="1000"
# IMAP_RECONNECT_MAX_MS="300000"
# IMAP_SERVICE_DEBUG="false"

# Trash purge scheduler (runs inside IMAP Service / worker container)
# Enable scheduled purging of emails in Trash after user retention days.
# TRASH_PURGE_ENABLED="1"

# Cron expression (default: 10 3 * * *)
# TRASH_PURGE_CRON="10 3 * * *"

# Timezone (IANA name). If empty, uses `TZ` inside the container.
# TRASH_PURGE_TIMEZONE=""

# Dry run (log only; no deletions)
# TRASH_PURGE_DRY_RUN="0"

# Optional limit per user (positive integer)
# TRASH_PURGE_LIMIT=""

# Optional: set container timezone (used by cron if TRASH_PURGE_TIMEZONE is empty)
# TZ="UTC"

# ======================
# Storage (file system)
# ======================
# Base path for file storage (raw emails, attachments).
# Docker: /data/storage
# Development: ./data/storage
# STORAGE_PATH="./data/storage"

# Maximum attachment size in bytes (default: 25MB = 26214400)
# ATTACHMENT_MAX_SIZE="26214400"

# ======================
# Dev helpers (optional)
# ======================
# Used by `scripts/test-webhook.ts` only.
# TEST_WEBHOOK_SECRET=""
